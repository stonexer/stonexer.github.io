{
  "name": "stonexer.github.io",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://www.sitixi.com",
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-particles-js": "^3.5.3",
    "react-scripts": "4.0.3",
    "react-spring": "^9.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -r ./blog ./build && cp ./CNAME ./build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "gh-pages -d build -b master"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
